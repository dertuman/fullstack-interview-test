// This information is not used but left for illustration purposes in case its needed later

// const router = require('express').Router();
// let PullRequest = require('../models/pullrequest.model');

// router.route('/').get((req, res) => {
//     PullRequest.find()
//     .then(pullrequest => res.json(pullrequest))
//     .catch(err => res.status(400).json('Error: ' + err));
// });

// module.exports = router;